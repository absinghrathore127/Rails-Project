<%= form_for(@account_activation) do |f| %>
  <% if @account_activation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account_activation.errors.count, "error") %> prohibited this account_activation from being saved:</h2>

      <ul>
      <% @account_activation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
